<%- include header %>
      
      <div class="row">
        
        <div class="col-md-6 event-image-wrap">
            
          <div class="event-image" style="clear:both">
            <img src="<%= event.images[0].path %>" alt="<%= event.name %>">
          </div>

        </div>

        <div class="col-md-6 <% if (event.temp) { %> event-temporary <% } %>">

          

          <div class="event-name">

            <h1 class="ff-cabin"><%= event.name %></h1>

          </div>

          <div class="event-meta">
            
            <div class="meta-row">
              <i class="fa fa-calendar-o"></i>
              <%= moment(event.date).format('LLLL') %>
            </div>

            <div class="meta-row">
              <i class="fa fa-map-marker"></i>
              <%= event.location %>
            </div>

          </div>

          <div class="event-description ff-nobile">

            <%- marked(event.description) %>
            
          </div>

          <div class="event-toggle-description">
            <a>Read more</a>
          </div>
          
          <div class="event-form"> 
            
            <div class="form-error">

              <h4> <i class="fa fa-exclamation-triangle"></i> Hmmm, something's not right</h4>
              
              <p>Looks like there was an error.</p>

              <p>You can try again a bit latter or just shoot us an email and we'll set up your reservation manually: contact@reservr.com.</p>

            </div>
            
            <div class="form-success">

              <h4> <i class="fa fa-check"></i> Success!</h4>

              <p>Your reservation was made and <br> a confirmation email was sent to your email address.</p>

              <p>Click the link bellow to manage your reservation.</p>

              <a href="http://www.reservr.net/r/MQnKlp6Fzwv4MNdp" target="_blank">http://www.reservr.net/r/MQnKlp6Fzwv4MNdp</a>
              
            </div>

            <div class="row">

              <div class="col-md-6">
                <a class="btn btn-toggle-fields btn-primary btn-lg">Reserv seats</a>

                <div class="event-reserv-fields">

                  <div class="form-group">
                    <label for="">Seats:</label>
                    <select name="" id="" class="form-control input-lg">
                      <option value="1">1</option>
                      <option value="2">2</option>
                      <option value="3">3</option>
                      <option value="4">4</option>
                      <option value="5">5</option>
                    </select>

                  </div>
                  <div class="form-group">

                    <input type="text" name="" class="form-control input-lg" id="" placeholder="Name">

                  </div>

                  <div class="form-group">

                    <input type="email" name="" class="form-control input-lg" id="" placeholder="Email">

                  </div>

                  <div class="form-group">
                    <a class="btn btn-reserv btn-primary btn-lg <% if (event.temp) { %> disabled <% } %>">Reserv seats</a>
                  </div>

                  <% if (event.temp) { %>
                    <p class="text-muted">You'll need to <a href="/signup">create an account</a> to enable this feature, so you can check who signed up for your awesome event.</p>
                  <% } %>

                </div>
              </div>
              
              <div class="col-md-6">
                
                <span class="event-price ff-montserat">
                  <%= event.price %>
                </span>

                <p class="text-muted"><span class="seats-invited">0</span> seats taken of <span class="seats-total"><%= event.seats %></span> available</p>

              </div>
            </div>
          </div>
      </div>
    </div>
  <%- include footer %>